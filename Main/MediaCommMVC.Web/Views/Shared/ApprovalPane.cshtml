@model MediaCommMVC.Web.Core.ViewModel.ApprovalPaneViewModel
<div class="approvalContainer">
    <span class="approvalButton">
        @if (this.Model.ShowButton)
        {
            <a class="button" data-url="@this.Model.Url">@Resources.Approval.IApprove</a>
        }
    </span>
    <span class="approvals" data-url="@this.Model.Url">
        
    </span>
</div>
