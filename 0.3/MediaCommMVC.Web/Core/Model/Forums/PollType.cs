namespace MediaCommMVC.Web.Core.Model.Forums
{
    /// <summary>Defines the type of the poll.</summary>
    public enum PollType
    {
        /// <summary>Multiples answers per user are allowed.</summary>
        MultiAnswer, 

        /// <summary>Only one answer per user allowed.</summary>
        SingleAnswer
    }
}